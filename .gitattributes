# Normalize text files to LF in the repo so checks are identical everywhere.
# The pre-commit hook MUST check out with LF: Git for Windows defaults to
# core.autocrlf=true, and a CRLF shebang kills the hook ("bad interpreter").
* text=auto
.githooks/* text eol=lf
*.py text eol=lf
*.md text eol=lf
